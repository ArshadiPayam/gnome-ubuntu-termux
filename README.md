# gnome-ubuntu-termux

```
apt-get update -y;apt-get -o DPkg::Options::="--force-confnew" -y upgrade;pkg install x11-repo -y;apt-get update -y;apt-get install wget -y;wget https://github.com/ArshadiPayam/gnome-ubuntu-termux/raw/main/ubuntu-install.sh;bash ubuntu-install.sh
```
