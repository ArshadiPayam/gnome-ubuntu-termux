# gnome-ubuntu-termux

```
pkg update -o DPkg::Options::="--force-confnew" -y;pkg install x11-repo -y;pkg update -y;pkg install wget -y;wget https://github.com/ArshadiPayam/gnome-ubuntu-termux/raw/main/ubuntu-install.sh;bash ubuntu-install.sh
```
